---
const { current = '' } = Astro.props;
---

<style>
  nav {
    align-items: center;
    display: flex;
    flex: 1;
    gap: 4ch;
    font-family: var(--font-family-monospace);
    font-weight: 700;
    justify-content: center;
    text-transform: lowercase;
  }

  a {
    color: inherit;
    padding: 10px 5px;
    display: block;
    position: relative;
    min-width: 70px;
    text-align: left;
  }

  a:not(.selected) {
    opacity: 0.87;
  }
</style>

<nav>
  <a class={current === "thespace" ? "selected" : ""} href="/thespace">🛠️ the space</a>
  <a class={current === "membership" ? "selected" : ""} href="/membership">💡 get involved</a>
  <a href="https://community.canberramaker.space/" target="_blank">🗩 forum</a>
  <a href="https://www.instagram.com/canberra.makerspace" target="_blank">🗪  discord</a>
</nav>
